## 2026-09-17 · 기록 입력 대행과 프론트 전면 개편

- 사용자가 요청하거나 승인하면 AI가 계획·할 일·실행 기록의 초안을 만들고 서비스에 입력할 수 있다. AI 대행이라는 이유만으로 실제 자료에서 제외하지 않는다.
- 실제로 수행한 활동의 시작·종료 시각, 소요 시간, 막힌 이유와 사용자의 회고·판단은 사용자가 제공하거나 확인한 사실만 기록한다. 검사용·데모 자료를 실제 수행 증거로 바꾸어 주장하지 않는다.
- 프론트는 기존 정보 구조와 주소 호환을 유지하지 않고 전면 개편할 수 있다. 최신 사용자 확정안은 [프론트 전면 개편 명세](docs/frontend-redesign-2026-09-17.md)를 따른다.
- 화면에 검사용·데모 자료의 별도 표식을 강제하지 않는다. 다만 검증 증거에는 실제 자료와 검사용 자료의 출처를 구분한다.

## 2026-09-14 · NAS web-server 이전 완료
사용자 요청에 따라 PlanDoSee의 프론트·API·DB를 Vultr에서 NAS의 web-server VM으로 이전했다. 운영 주소는 [plandosee.app](https://plandosee.app) 그대로다. Cloudflare의 기본 도메인 A 레코드를 211.194.175.156으로 변경했고 DNS only와 기존 메일 인증 레코드를 유지했다. 일반 외부 HTTPS 요청의 도착 IP가 NAS임을 확인했다.
Next.js·Spring·MariaDB는 원본과 동일한 이미지로 실행한다. DB 22개 표의 전체 행을 정렬한 덤프 SHA-256이 원본과 일치했고 Flyway V1~V4, DB 계정 권한도 대조했다. 사용자 1·계획 2·할 일 1·실행 기록 0은 전체 DB 집계이며 과제의 실제 기록 요건 충족을 뜻하지 않는다.
화면·정적 자산·API·문서·개인 API 401·CSRF 403/잘못된 입력 400·보안 쿠키 등 HTTP 검사 16개와 브라우저 랜딩/기존 공개 계획 표시를 확인했다. 초기 복원 스크립트에 V4 표의 쓰기 권한이 빠져 잘못된 입력 검사에서 401이 발생했으며, 원본의 실제 권한 복원 후 같은 검사가 400으로 통과했다. 실제 기록이나 시험 메일은 생성하지 않았다.
Vultr 158.247.203.145의 앱·DB·프록시는 모두 중지했고 원본 볼륨과 백업은 보존했다. 인스턴스 삭제와 과금 종료는 수행하지 않았다. 소셜 로그인 보류와 전체 과제 인수 미완료는 유지한다. NAS 재부팅 복구·실제 계정 로그인/메일 실수신·외부 정기 백업·공인 IP 자동 갱신은 이번에 검증하지 않았다.

상세 운영 절차: [NAS 이전 기록](docs/nas-migration-2026-09-14.md).

아래는 이전 시점의 기록이다. ‘프론트 미구현’, ‘Vultr 현재 운영’, /opt/pds와 과거 검사 수는 당시 상태이며 현재 운영은 위 2026-09-14 기록을 따른다.

# PDS Diary 필수 RULE

2026-09-09 최신 상태: 공개/개인 웹을 [운영 주소](https://plandosee.app)에 배포했다. 계획/이력·할 일·실행/완료·검색/필터/정렬·돌아보기/근거·다음 계획·전체 JSON·휴지통/복제·이메일 계정·알림 설정을 연결했다. 타입/Windows·Linux 빌드, 단위13·격리 API/DB17·운영 HTTPS16개를 통과했다. 사용자 최신 지시로 소셜 로그인은 명시적으로 다시 요청할 때까지 작업을 보류한다. 실제 사용자 1/5/3·최종 DB 계약·전체 인수는 미완료. 상세: [웹 구현·검증](../PDC_Diary_Nextjs/docs/full-implementation.md). 아래 이전 단계 기록은 당시 상태다.

최신 배포 증거: [DB·백엔드·메일 배포 진행](docs/deployment-status.md) — 내부 API·21개 표 복원·메일 수신 확인, 공개 DNS/HTTPS·API·Swagger 확인 완료, 프론트는 미구현.


기준일: 2026-09-08 / 상태: 백엔드 구현·로컬 시험 진행, 배포·전체 과제·실제 사용자 기록 검증 전. 실제 증거는 docs/backend-verification.md에 기록한다.

## 효력과 완료 판정

1. 사용자가 제공한 아래 조건은 모두 필수다. 중요도나 시간 부족을 이유로 하나도 생략·완화하지 않는다.
2. 이 문서는 개발 규칙이다. RULE 문서만 있다고 충족된 것이 아니며, 실제 배포 결과와 증거가 있어야 통과다.
3. 추적 상태는 `미구현 → 구현됨 → 검증됨`으로 관리한다. 실패·미확인은 별도 기록한다. 44개 조건 및 번호 없는 조건 중 하나라도 미검증·실패이면 과제 전체 완료로 보고하지 않는다.
4. 설명 없는 번호를 임의로 채우지 않는다. 제공된 ID는 C01, C04~C36, C57~C60, C78~C83의 총 44개다.
5. [요구사항 원문 및 검증 연결](docs/requirements.json)의 44개 `text`는 본 RULE의 일부다. 모든 항목은 동일한 강제력을 가진다. 변경하려면 사용자 요구 변경을 명시적으로 확인하고 문서·계약·검증을 함께 갱신한다.

## 문서·노션 동기화

2026-09-08 사용자 지시: 앞으로 계획 등을 수정할 때는 로컬 문서뿐 아니라 노션도 함께 수정한다.

2026-09-18 사용자 후속 지시: 현재 프론트 시안 조율 단계에서는 노션 작업을 보류하고 로컬 Markdown 문서만 정리한다. 시안이 충분히 확정된 뒤 사용자가 요청하면 노션 원문과 로컬 문서를 한 번에 대조·동기화한다. 이 보류 기간에는 `docs/notion-content.json`, `docs/notion-publication.json`을 포함한 노션 동기화 산출물도 갱신하지 않는다. 이 지시는 아래의 상시 동기화 절차보다 우선하며, 사용자의 후속 지시로 다시 해제할 수 있다.

- 계획·기능·기술·인프라·일정·RULE 변경 시 관련 로컬 Markdown·JSON 문서와 해당 노션 하위 문서를 같은 작업에서 갱신한다. 내용이 영향받는 계약도 함께 맞춘다.
- 2026-09-08 사용자의 후속 지시로 PDF를 삭제하고 관리 대상에서 제외한다. 사용자가 다시 요청하기 전에는 PDF를 생성·갱신·검증하지 않는다. `docs/pds-design.md`는 직접 편집하고 이전 PDF 생성·검증 스크립트는 실행하지 않는다.
- 노션 목차: https://app.notion.com/p/3d50def9f6268037adfbcef233c6d20c . 하위 문서 URL·ID는 `docs/notion-publication.json`에서 확인한다. 목차 구조가 바뀌면 링크도 함께 갱신한다.
- 노션의 최신 내용을 먼저 읽어 사용자가 직접 수정한 내용을 보존한다. 필요한 부분만 간결하게 수정하고 저장 후 재조회하여 로컬 문서와 일치하는지 확인한다.
- 변경된 노션 문안은 `docs/notion-content.json`에 반영하고, 동기화 결과·변경 페이지는 `docs/notion-publication.json`에 기록한다.
- 사용자 후속 지시: 설명과 중간 결과를 자주 제공하고 매 작업 단계의 결정·변경·실제 검증 결과·남은 일을 로컬 문서와 노션에 항상 기록한다. 사용자 확정과 AI 추천을 구분하며 비밀값·개인 기록 원문은 진행 기록에 복사하지 않는다.
- 접속·권한·저장 오류로 한쪽이 갱신되지 않으면 미동기화 항목과 이유를 기록하고 사용자에게 알린다. 양쪽 반영 확인 전에는 문서 수정 완료로 보고하지 않는다.

## AI 작업 시 문맥 확인 절차

2026-09-09 사용자 후속 지시에 따른 필수 작업 규칙. 설계·구현·검증·제출 작업에 적용한다.

1. 대상 저장소의 적용 AGENTS.md와 중앙 RULES.md를 읽는다. 공통 현재 설계·사용자 후속 결정·작업 도메인의 계약 원문도 확인한다.
2. 저장소 목록과 변경 영역으로 관련 저장소를 정한다. 기본 설계와 대상 구현 저장소를 함께 참고하고, 관련 프런트·백엔드·Security·Optimization의 채택된 결정이 있으면 해당 원문을 추가로 읽는다. 저장소 개수로 필요한 근거를 제한하지 않는다.
3. 파일 목록·키워드 검색으로 필요한 원문을 찾고, 정본·현재 작업 버전·미커밋 변경·현재/과거 상태를 구분한다. 필수 RULE을 검색 상위 결과나 요약으로 대체하지 않는다. 다른 저장소의 문서를 링크만 보고 읽었다고 간주하지 않는다.
4. 수정 전에 읽은 주요 출처와 적용할 조건·변경 범위를 간결하게 밝힌다. 필요한 원문이 없거나 서로 충돌하면 불확실성을 알리고, 해결되지 않은 조건에 의존한 수정을 추측으로 진행하지 않는다.
5. 작업 재개·문맥 압축·설계 변경 후에는 변경 대상의 필수 조건과 현재 원문을 다시 확인한다. 과거 대화의 기억만으로 규칙 준수를 판단하지 않는다.
6. 변경 후 관련 계약·회귀 검사를 실행하고 실제 결과를 기록한다. 문서만 수정한 경우에는 원문/링크·동기화 재조회 등 해당 변경에 맞는 확인을 한다. 적용 조건·출처·검사 결과·남은 일과 변경된 설계 문서를 로컬/노션에 함께 반영한다.

기본 방식은 **필수 원문 직접 읽기 + 관련 파일 검색 + 변경 후 검증**이다. 별도 벡터 RAG 구축은 필수 준비물이 아니며, 근거 누락·탐색 비용이 실제로 커지면 추가 효과를 평가한다. 상세 설명과 과거 제안은 docs/context-management.md에 둔다.

## 2026-09-17 프론트 전면 개편 필수 구현 RULE

아래 항목은 사용자 후속 지시로 확정한 프론트 구현 원칙이며 선택 사항이 아니다. 프론트 설계·시안·구현·검증을 시작하거나 문맥 압축 뒤 재개할 때 이 절과 [프론트 컴포넌트 수정 현재 상태](../PDC_Diary_Nextjs/docs/redesign/frontend-component-editing-state-2026-09-17.md)를 먼저 읽는다. 사용자가 후속 지시로 바꾸기 전에는 임의로 생략·완화·대체하지 않는다.

이 RULE은 고정 불변의 규칙이 아니며 **사용자의 후속 지시로 언제든지 쉽게 변경·추가·삭제할 수 있다.** 별도의 복잡한 승인 절차 없이 사용자의 최신 명시적 지시를 즉시 정본에 반영하고, 충돌하는 이전 내용은 최신 지시에 맞춰 수정한다. 다만 AI가 사용자 지시 없이 임의로 RULE을 바꾸거나 완화해서는 안 된다.

### 구조와 정보 범위

- 2026-09-18 사용자 확정 작업 순서: 승인된 메인 화면을 기준으로 제품의 모든 화면과 공통 상태를 먼저 디자인하고 각각 시안 파일로 저장한다. 데스크톱 16:9·320px 모바일·밝음/어두움·핵심 인터랙션과 모션을 화면별로 확인·승인한 뒤, 전체 백엔드 계약을 확정하고 실제 제품 구현을 시작한다. 상세 목록과 상태는 [프론트 전체 화면 시안 제작·구현 순서](../PDC_Diary_Nextjs/docs/redesign/frontend-screen-production-plan-2026-09-18.md)를 따른다.
- 기존 프론트 UI와 예전 주소 호환은 버릴 수 있지만 저장된 사용자 자료와 서버 도메인 계약은 복원·보존한다.
- Plan·Do·See는 페이지 이름으로 강제하지 않고 `계획 → 할 일 → 실행 근거 → 통계·회고 → 다음 계획`이 한 흐름으로 이어지게 한다.
- 로그인 사용자의 기본 작업실은 스프린트 대시보드이며 기본 흐름은 `오늘 할 일 → 스프린트 보드 → 전체 일정/캘린더`다. 통계와 회고는 별도 화면으로 두되 접근하기 쉽게 한다.
- 회고는 `사실 확인 → 다음 → 고칠 점 작성·확인 → 다음 스프린트를 만들지 선택`의 순차 흐름으로 진행한다. 처음에는 사실 확인만 보이고, 다음 단계에 도달하면 기존 컴포넌트를 없애지 않은 채 바로 아래에 새 컴포넌트를 쌓는다. 고칠 점을 확인하기 전에는 다음 스프린트 선택을 열지 않으며 마지막 선택은 `지금 만들기 / 나중에 만들기`를 명시한다.
- 회고의 `사실 확인 / 고칠 점 확인 / 다음 스프린트` 세 플로우는 각각 같은 폭의 독립 카드로 만들고 한 열에 세로로 쌓는다. 다음 단계에 도달하면 기존 카드를 유지한 채 바로 아래에 새 카드를 추가하며 카드 사이에 일정한 여백을 둔다. 사실 확인이 계속 보이므로 고칠 점 카드의 `사실 다시 보기` 버튼은 두지 않는다. 다음 스프린트에 전달할 고칠 점 요약은 굵은 왼쪽 선 없이 네 모서리를 모두 둥글게 처리한다.
- 회고에서 통과한 플로우 카드는 반투명 흰색 면을 덧씌우고 `완료` 표시를 함께 보여 현재 플로우와 명확히 구분한다. 완료 표현은 원래 카드의 크기와 누적 위치를 바꾸지 않는다.
- 사실 확인과 고칠 점 확인 플로우의 다음 단계 행동 문구는 모두 `다음으로`로 통일한다. 두 버튼은 같은 너비·높이·좌우 패딩을 사용하고 문구의 가로·세로 중심을 맞춘다. 단계를 통과한 뒤에는 버튼을 비활성화하고 작은 `완료` 상태 문구로 낮춘다.
- 회고 사실 확인은 균등 수치 표, 완료율 링, 비대칭 요약 보드, 한 문장형 요약을 사용하지 않는다. `일의 진행 / 쓴 시간 / 도움이 필요했던 일`은 사실 확인 플로우 카드 내부에서 각각 독립된 작은 카드로 구분해 한 열에 세로로 쌓는다. 내부 카드 세 개는 같은 폭·시작선·핵심값 끝선을 사용하고, 바깥 플로우 카드보다 낮은 배경·그림자 강도로 계층을 구분한다. 여러 기록 중 임의의 한 건만 대표로 펼치지 않는다. `일의 진행` 완료율은 경험치 바 형태로 표시하며 `75% 이상=초록, 50~74%=주황, 49% 이하=빨강`으로 구분한다. 예상보다 더 쓴 시간은 주황, 덜 쓴 시간은 초록으로 강조한다. `도움이 필요했던 일` 카드를 누르면 해당 항목 전체 목록을 그 작은 카드 안에서 높이·위치·불투명도 모션으로 아래에 펼친다.
- 다음 스프린트 선택은 큰 전달 색상면, 아이콘 선택 카드, 좌우 분할 패널, 큰 전달 문장 중심 구성을 사용하지 않는다. 확인한 고칠 점·출처를 한 줄 기록으로 먼저 보여 주고 바로 아래의 결정 바에 `다음 스프린트로 이어갈까요?` 질문과 `지금 만들기 / 나중에 만들기` 두 행동만 둔다. 두 행동은 같은 너비·높이·테두리·라운드를 사용하고, 선택된 행동 하나만 공통 초록 채움과 흰 글자로 표시한다. 선택 전에는 추천 행동인 `지금 만들기`만 같은 선택 스타일을 미리 보여 준다. 모바일에서도 두 행동은 같은 폭으로 배치한다.
- `지금 만들기`를 누르면 생성 입력을 결정 카드 안에 붙이지 않고 바로 아래의 별도 카드로 열며, 첫 자유 입력인 스프린트 이름에 자동 포커스한다. 생성 카드의 이전 단계 행동은 `고칠 점 수정`이 아니라 `돌아가기`로 명시하고, 누르면 생성 카드를 닫은 뒤 고칠 점 카드와 입력에 포커스를 돌린다.
- 회고의 마지막 `다음 스프린트` 카드와 아래의 생성 카드는 다른 플로우 카드와 같은 1px 중립 경계·12px 모서리·그림자를 사용한다. 현재 단계라는 이유로 마지막 카드 테두리만 초록색이나 이중 링으로 강조하지 않는다. 생성 폼의 우선순위는 세 값을 항상 펼쳐 놓지 않고 현재 값 하나를 보여 주는 선택 버튼으로 두며, 누르면 `높음 / 보통 / 낮음` 목록을 아래로 펼쳐 하나를 고르게 한다.
- 다음 스프린트 기간 선택기는 시작일과 종료일을 고른 직후 자동으로 접지 않는다. 선택 범위와 요약을 달력에서 확인할 수 있게 열린 상태를 유지하고, 명시적인 `선택 완료` 행동으로 닫는다. 시작일만 고른 상태에서는 `선택 완료`를 비활성화한다.
- 왼쪽 계획 선택기에서 전체 스프린트와 개별 계획을 전환한다. 개별 계획을 선택하면 본문과 오른쪽 보조 영역이 모두 같은 계획 범위를 다루고, 전체 스프린트에서는 모든 계획의 할 일을 소속 계획과 함께 보여 준다.
- 전체 스프린트의 마감 필터는 목록 도구 줄에서 `필터` 바로 왼쪽에 같은 높이·버튼 규격으로 둔다. 별도 목록·메뉴·오른쪽 화살표를 열지 않는 단일 버튼이며 누를 때마다 `전체 → 오늘 → 일주일 → 이번달 → 전체` 순서로 즉시 순환한다. 이 전환은 마감 버튼·목록 제목·할 일 목록만 부분 갱신하고 상단 활성 작업바를 다시 만들거나 시작 모션을 재생하지 않는다. 캘린더는 주간·월간만 제공한다. 월간 요일은 일→토, 날짜 셀은 정사각형, 선택 날짜 글자는 흰색이다.

### 시각·반응형 원칙

- 모든 디자인은 사용자가 편하게 쓸 수 있어야 한다. 날짜·기간·우선순위·상태처럼 유효한 선택지가 정해져 있거나 선택 UI로 처리할 수 있는 값에는 반드시 알맞은 선택기를 제공한다. 사용자가 직접 타이핑해야 하는 입력은 이름·설명·회고 문장처럼 자유 입력이 반드시 필요한 경우로만 제한하며, 선택기로 대체 가능한 값을 텍스트 입력기로 만들지 않는다.
- Jira처럼 진행 상황과 정보 밀도가 한눈에 보이게 하되 개발 도구처럼 복잡한 메타데이터와 강한 파란 면은 가져오지 않는다.
- 기본 글꼴은 제목과 본문 모두 SUIT로 통일하고 따뜻한 기본 분위기를 유지한다. 진한 녹색은 넓은 면이 아니라 강조 글자·초점·핵심 행동에만 최소한으로 사용한다.
- 모든 화면은 같은 의미에 같은 배경/전경 색상쌍을 사용한다. 채워진 초록 핵심 행동 버튼은 라이트 `#246B55`, 다크 `#2D715A` 배경과 **흰색 `#FFFFFF` 글자·아이콘**만 허용한다. 밝은 초록 배경에 진한 초록 글자를 쓰는 상태·선택 면은 별도의 soft 조합이며, 채워진 핵심 행동 버튼과 혼용하지 않는다.
- 채워진 의미색은 위험 `#A84646 / #FFFFFF`, 경고 `#995719 / #FFFFFF`, 정보 `#3E6EA8 / #FFFFFF`, 중립 `#35403B / #FFFFFF`로 고정한다. soft 상태면은 라이트에서 성공 `#DCECE5 / #174C3C`, 경고 `#F8E8D8 / #7E4614`, 위험 `#F7E3E3 / #863535`, 정보 `#E6EEF8 / #315D91`, 중립 `#E9EDE9 / #46504B`; 다크에서 성공 `#203A31 / #A9E3CC`, 경고 `#482F1C / #F1C38F`, 위험 `#452526 / #F4B2B2`, 정보 `#1E3044 / #A8C9F0`, 중립 `#2A322F / #C5CEC9`를 사용한다.
- **버튼 색상 조합은 역할별로 강제한다.** 핵심 실행·확정 행동은 `채운 초록 배경 + 흰 글자·흰 아이콘`, 일반 보조 행동은 `surface 배경 + 기본 글자·아이콘 + 중립 경계`, 현재 선택·활성 상태는 `soft 성공 배경 + 성공 전경`, 삭제·중단 같은 위험 행동은 `위험 채움 + 흰 전경` 또는 위계가 낮을 때만 `surface/soft 위험 배경 + 위험 전경`, 비활성 행동은 중립 배경·중립 전경으로 표시한다. 초록 채움 위에 초록·검정·회색 글자나 아이콘을 놓지 않으며, 글자와 아이콘이 서로 다른 전경색을 갖지 않는다. 외곽선 초록 버튼은 `surface 배경 + 초록 전경 + 초록 경계`를 사용하고 hover 때만 soft 성공 배경으로 바꾸며 핵심 채움 버튼처럼 보이게 혼용하지 않는다.
- 버튼의 기본·hover·active·focus·disabled·selected 상태와 라이트·다크 모드 모두에서 같은 의미 조합을 유지한다. 새 화면·컴포넌트를 제출하거나 사용자에게 띄우기 전에 실제 브라우저에서 버튼별 계산 배경색·글자색·아이콘색을 라이트와 다크로 확인하고, 채운 초록 버튼의 전경이 `#FFFFFF`가 아니거나 의미 역할과 조합이 다르면 미완료로 처리한다.
- 새 화면·상태·컴포넌트가 임의의 유사색을 추가하지 않도록 공통 의미 토큰을 사용하고, 글자와 아이콘은 해당 전경 토큰을 함께 상속한다. 색상만으로 상태를 전달하지 않으며 문구·아이콘·형태를 병행한다. 모든 일반 크기 텍스트 색상쌍은 WCAG AA 대비 4.5:1 이상을 실제 계산 또는 브라우저 검사로 확인한다.
- 아이콘은 SVG 계열로 사용하고 크기·기준선·상하좌우 정렬을 맞춘다. 버튼·태그를 불필요하게 크거나 둥글게 만들지 않는다.
- 16:9 넓은 화면과 아이폰 미니 수준의 320px 화면을 동시에 설계한다. 데스크톱을 만든 뒤 모바일을 축소판으로 붙이지 않는다. 기본 UI 밀도는 현재 시안의 약 90%다.
- **화면 비율별 실제 브라우저 검사를 생략하지 않는다.** 새 화면·레이어·팝오버·코치마크·반응형 변경은 최소 `16:9 데스크톱(1440×810) / 4:3 중간 화면(1024×768) / 9:16 모바일(320×568)`에서 직접 렌더한다. 각 비율마다 가로 넘침, 잘림, 대상과 오버레이의 상·하·좌·우 간격, 모서리 반경·테두리 굵기 비율, 카드 위치와 핵심 행동 노출을 계산값과 화면으로 확인하고 결과를 체크포인트에 기록한다. 한 비율의 통과 결과를 다른 비율의 통과로 간주하지 않으며, 세 비율 중 하나라도 미확인 또는 실패면 사용자 제시·승인·완료로 보고하지 않는다.
- 라이트·다크 모드를 모두 포함하며 선택은 계정별로 저장·복원한다.
- 스크롤 가능한 목록은 브라우저 기본 동작과 접근성을 유지하면서 얇은 테마 대응 스크롤바, 둥근 손잡이, hover 색상, overscroll 억제를 적용한다.

### 인터랙션과 모션

- 가능한 모든 클릭 요소에 hover·press·focus·열림·닫힘·대기·성공·실패 피드백을 둔다. iOS처럼 부드럽게 느껴지되 과장되거나 느리지 않게 하고 `prefers-reduced-motion`을 지원한다.
- 컴포넌트 전환에서 DOM을 통째로 교체해 화면이 튀게 만들지 않는다. 같은 요소의 높이·위치·간격·불투명도·화살표·메뉴가 연속적으로 바뀌는 구조를 우선한다. 단순 페이드만으로 상태 변화를 대신하지 않는다.
- 전체 스프린트의 마감 필터처럼 목록 결과가 바뀌는 부분 갱신에도 짧은 이동·불투명도 전환을 일관되게 적용한다. 이때 필터 대상이 아닌 상단 활성 작업바는 DOM과 표시 상태를 유지한다.
- 삭제는 확인창을 사용한다. 일반 상태 변경은 조용히 즉시 반영하고, 이동은 즉시 반영한 뒤 되돌리기를 제공한다. 중요한 결과는 화면 상단 알림으로 알린다.
- 마우스·터치 드래그로 별도 순서 변경 모드 없이 순서를 바꾼다. 키보드 전용 순서 변경 모드는 만들지 않지만 Tab·Enter·Space·Escape, 레이블, 보이는 포커스는 지원한다.

### 할 일 컴포넌트

- 할 일 컴포넌트와 작업바 컴포넌트는 분리해서 수정하며 현재는 할 일 컴포넌트를 먼저 완료한다. 작업바는 할 일 컴포넌트가 사용자 확인을 받은 뒤 별도 단계에서 수정한다.
- 시안 기준은 `plandosee-sprint-list-detail-16x9.html`의 롤백된 전체 원본 화면이다. 별도 단독 컴포넌트를 비슷하게 재구성하지 않고 전체 원본 안의 대상 컴포넌트만 수정한다.
- 접힌 할 일 행은 원본의 낮고 간결한 구조를 유지한다. 펼침·접힘 과정에서도 헤더를 통째로 교체하지 않고 같은 구조가 부드럽게 전환돼야 한다.
- 본문 미리보기는 최대 3줄이며 실제 렌더 결과가 3줄을 넘을 때만 중앙에 몸통 없는 화살표를 표시한다. 대표 긴 본문은 `전체 스프린트에서 여러 계획의 할 일을 빠르게 비교하고, 선택한 할 일의 핵심 정보와 실행 기록을 한곳에서 확인한다.` 뒤에서 명시적으로 다음 줄을 시작해 넓은 화면에서도 문장이 지나치게 길어지지 않게 한다.
- 메타정보는 별도 컨테이너 카드가 아닌 작은 해시태그 형식으로 둔다. 우선순위는 상세 하단에서 빼고 헤더의 마감 왼쪽으로 옮긴다. 접힌 행에서는 `#높음 / #보통 / #낮음`, 펼친 행에서는 마감 배지와 같은 밀도의 `우선순위 높음 / 보통 / 낮음`으로 전환한다. 높음·보통·낮음은 각각 다른 안정된 색을 쓰되 문구를 함께 표시하고 우선순위 배지에는 테두리를 두지 않는다. 상세 하단 순서는 `마감 시각 → 예상 시간 → 실제 시간`이다.
- 펼친 상세의 해시태그 메타는 왼쪽 내용 열의 하단에 정렬한다. 여러 줄로 감기면 오른쪽 작업 실행 패널은 같은 상세 행 높이까지 함께 늘어난다.
- 펼친 헤드 오른쪽에는 `우선순위 → 마감 상태 → 변경 가능한 진행 상태 → 편집 버튼`만 둔다. 네 컨트롤은 22px 높이와 같은 중심선을 사용한다. 진행 상태 글자 크기·굵기는 `오늘마감`과 같게 하고 선택 버튼 안에는 방향 아이콘을 두지 않는다. 편집 아이콘은 12px로 글자와 균형을 맞춘다. 컨트롤 박스는 내용에 맞춰 유지한다. 진행 상태를 누르면 상태 변경 메뉴가 열려야 한다. 상태 생명주기는 `예정 → 진행 중 → 완료`이며 막힘은 별도 상태가 아니라 배지·필터 개념이다.
- 접힌 행의 진행 상태 박스는 상태 문구 길이에 관계없이 48px 고정 너비를 유지하고 텍스트를 상하좌우 중앙에 정렬한다. 접힌 상태에서는 진행 상태 화살표를 숨기는 수준이 아니라 렌더링 공간과 간격까지 제거하며, 상태 변경 동작은 박스 전체 클릭으로 유지한다.
- 접힘·펼침 진행 상태 선택 버튼은 문구 외에도 같은 색으로 상태를 보조한다. `완료`는 초록 계열, `진행 중`은 주황 계열, `예정`은 기존 중립색을 사용한다.
- 마감 태그는 단계별 색을 쓴다. 현재 시안의 기본 매핑은 오늘=빨강, 내일·이번 주=주황, 이후=파랑, 완료된 할 일의 마감=초록이며 헤더와 상세 하단 태그에 동일하게 적용한다. 정확한 색조는 사용자 후속 지시로 바꿀 수 있다.

### 작업 시간과 작업바

- 용어는 다음과 같이 고정한다. `작업탭`은 펼친 할 일 내부의 실행 패널이고, `작업바`는 본문 섹션 최상단에 쌓이는 전역 작업 상태 줄이다.

- 사용자가 플랫폼을 계속 열어 두거나 일반 스톱워치처럼 시작·정지를 반복하게 만들지 않는다. 할 일에서 기본 1시간 작업을 시작하고 서버 시각·시작 시각·예정 종료 시각으로 시간을 복원한다.
- 할 일의 예상 작업시간은 **선택 입력**이며 작업 시작 조건이 아니다. 예상시간을 입력하지 않아도 곧바로 작업을 시작할 수 있다. 기본 1시간은 할 일의 예상값이 아니라 작업 세션의 최초 자동 종료 기준이며, 예상시간 필드를 자동으로 채우거나 덮어쓰지 않는다.
- 작업탭의 `1시간 작업 시작` 버튼은 외곽선만 있는 재생 아이콘이 아니라 내부가 초록 포인트색으로 채워진 재생 아이콘을 사용한다.
- `작업 시작`을 누르면 해당 할 일의 진행 상태를 즉시 `진행 중`으로 함께 바꾼다.
- 펼친 할 일의 실행 패널은 활성 작업일 때 제목을 `진행중`으로 바꾸고 부드러운 초록 계열 배경을 사용한다. 경과 시간은 `00:00:00` 형식으로 1초마다 갱신하며, 하단에는 색을 구분한 `중단`과 `30분 연장` 두 버튼을 둔다.
- 작업 시작 전·진행 중·확인 대기 실행 패널은 내용에 따라 늘어나지 않는 동일한 고정 외곽 높이를 사용하고 시간 글자 크기·행동 영역 높이도 유지한다. 새 활성 작업 탭과 실행 패널은 높이·이동·불투명도가 연속적으로 바뀌는 진입 모션을 쓰며 기존 작업 탭을 다시 재생하지 않는다. `중단` 문구는 같은 행동 줄의 다른 버튼보다 조금 더 굵게 표시한다.
- 활성 작업은 현재 본문 섹션 최상단의 얇은 작업바로 표시한다. 할 일 이름·진행 시간·자동 종료 시각·`중단`·`+30분 연장`을 제공하고 여러 작업이면 화면을 크게 가리지 않도록 세로로 쌓는다. 내부 여백·상태점·시간·행동 버튼은 할 일 행보다 과하게 커지지 않는 조밀한 규격을 쓴다.
- 중단 또는 자동 종료 뒤 계산 시간을 곧바로 확정하지 않고 상단 작업바와 펼친 할 일의 작업탭 모두에 `시간 수정`과 `기록 확인`을 제공한다. 작업탭에서 기존의 단일 `기록 확인 필요` 행동은 사용하지 않는다. 작업바의 기록 확인은 버튼 눌림 → 녹색 성공 강조와 `실행 기록 저장됨 / 통계 반영 완료` 문구 전환 → 작업바 축소·퇴장의 순차 모션을 사용한다. 작업탭에서 기록 확인을 눌러도 연결된 상단 작업바에 같은 성공·퇴장 모션을 재생한 뒤 제거하며, 작업탭 자체도 버튼 성공 피드백과 불투명도/크기 모션으로 전환한다. 확정된 시간만 예상값과 분리된 실행 기록으로 저장해 통계와 회고의 근거로 사용한다.

### 저장·온보딩·공개 경계

- 계정에는 테마, 코치마크 완료 여부와 버전, 기본 대시보드·캘린더 보기, 기본 정렬·그룹, 고정 필터·저장된 보기, 사이드바 접힘 상태를 저장하고 다른 기기에서 복원한다.
- 브라우저 세션에는 선택 스프린트, 검색·필터·정렬·그룹, 캘린더 위치, 선택 날짜·할 일, 펼친 카드·상세 패널, 목록 스크롤, 통계 근거, 회고 단계, 페이지 이동 중 입력을 유지한다.
- 새로고침하면 저장하지 않은 폼 입력, 드래그 중간 상태, 처리 중인 시각 애니메이션 상태를 폐기한다.
- 실제 첫 사용은 데이터가 채워진 예시 화면에서 시작하지 않는다. `스프린트 0개`인 빈 작업실에서 `첫 스프린트 만들기 → 첫 할 일 추가 → 예상 시간 입력 없이 기본 1시간 작업 시작 → 생성된 상단 작업바 확인`이 실제 요소 생성과 함께 순차 진행돼야 한다. 아직 존재하지 않는 할 일·작업바·통계·회고 요소를 미리 가리키지 않는다. 기존 전체 기능 8단계 안내는 첫 사용과 분리해 설정의 `기능 다시 보기`로 제공하고, 큰 안내 변경은 별도의 버전 변경 안내로 제공한다. 각 안내의 완료 여부와 버전은 계정에 저장한다.
- 데이터 없음은 로딩·오류·오프라인과 구분한다. `스프린트 없음 / 선택 스프린트의 할 일 없음 / 실행 기록 없음 / 선택 날짜 일정 없음 / 필터 결과 없음`을 각각 현재 맥락 안의 짧은 문구와 다음 행동으로 표시한다. 필터 결과 없음은 조건 초기화, 실행 기록 없음은 작업 시작 또는 할 일 이동, 일정 없음은 캘린더 유지, 첫 스프린트·할 일 없음은 해당 생성 행동을 제공한다. 실패·권한·오프라인 상태를 빈 데이터처럼 표현하지 않는다.
- 비로그인 공개 영역은 `체험실`, 로그인 개인 영역은 `내 작업실`이다. 체험실 진입 시 로그인하면 개인 일정을 관리하고 다른 기기에서 복원할 수 있다는 상단 토스트를 제공한다.

### 작업 기록 의무

- 프론트 작업의 사용자 확정·AI 추천·폐기안·미확정 항목·실제 검증 결과·남은 일을 [프론트 컴포넌트 수정 현재 상태](../PDC_Diary_Nextjs/docs/redesign/frontend-component-editing-state-2026-09-17.md)와 관련 설계·진행 문서에 매 단계 기록한다.
- 대화 요약이나 기억만으로 이어서 수정하지 않는다. 문맥 압축 또는 재개 뒤에는 위 체크포인트와 현재 시안 원문을 다시 읽고, 수정 대상 컴포넌트 밖의 영역은 건드리지 않는다.

## 번호 없는 필수 조건

- Plan → Do → See → 다음 Plan이 하나로 이어져야 한다. 체크 완료만 있는 도구로 끝내지 않는다.
- 실제로 하고 있는 운동·공부·업무·게임 연습·ALEPH 진행 중 하나를 사용자가 선택하거나 확인한다. 사용자가 요청·승인하면 AI가 계획 1개 이상, 그 계획의 할 일 5개 이상, 실행 기록 3개 이상의 초안을 만들고 입력할 수 있다. 실제 수행 시각·소요 시간·막힌 이유와 회고 판단은 사용자가 제공하거나 확인한 사실을 사용하며, 검사용·데모 자료를 실제 수행 증거로 세지 않는다.
- 계획 필드는 ID·기간·우선순위·성공 기준·예상 시간을 구현 전에 정한다. 같은 계획 ID를 유지하고 현재 값과 수정 이력을 분리한다. 최초 버전과 수정 전 버전은 불변이다.
- 할 일은 계획에 연결하고 생성·수정·완료·되돌리기·삭제·검색·필터·정렬을 제공한다. 마감일·우선순위·태그·예상 시간을 저장한다. 정렬 기준과 동률 처리 기준을 화면에 공개하고 서버에서 일관되게 적용한다.
- 시작·종료·실제 소요 시간·막힌 이유를 할 일에 연결한 별도 실행 기록으로 저장한다. 실행 저장은 계획과 할 일 예상값을 변경하지 않는다.
- 완료 버튼 연속 두 클릭을 직접 검증한다. 같은 요청 키, DB 고유 제약, 원자적 상태 변경으로 중복을 막는다. 버튼 비활성화만으로 처리하지 않는다. 완료 사건과 완료 집계가 각각 한 번만 증가해야 한다.
- 기간별 계획 수·완료 수·지연 수·막힘 수·예상 시간·실제 시간·차이를 표시한다. 숫자는 근거 기록으로 이동해야 한다. 고칠 점 한 줄을 다음 계획에 출처와 함께 넘긴다.
- 통계 UI에는 내부 계약 용어인 `근거`를 그대로 노출하지 않는다. `완료한 일 / 지연된 일 / 막힌 일 / 예상 시간 항목 / 실제 작업 기록 / 시간 차이 내역`으로 표시하되 같은 스냅샷의 기록 연결 계약은 유지한다. 핵심 수치 카드와 선택 항목 헤더에는 의미 아이콘·방향 화살표를 두지 않고 의미색 점과 문구를 사용한다. 주간 사실 요약은 완료·지연 수와 예상 대비 작업 시간의 두 줄로 제한하며, 예상/실제 막대는 중립색·공통 초록색과 테두리 없는 비교 영역을 사용한다.
- 예상·실제·차이 비교는 예상시간을 입력한 할 일과 그 할 일에 연결된 확정 실행 기록만 같은 대상 집합으로 대조한다. 통계에는 `예상 입력 수`와 `미입력 수`를 함께 표시해 비교 범위를 숨기지 않는다. 예상시간이 없는 할 일의 확정 실행 기록도 전체 실제 기록에서 사라지지 않으며 별도 전체 실제 합계·목록에서 조회할 수 있어야 한다.
- 기존 필수 집계의 `전체 실제 시간`과 `전체 차이 = 전체 실제 시간 - 입력된 예상 시간 합계`는 별도 계약값으로 보존한다. 사용자에게 주 비교값과 전체 집계값을 같은 이름으로 섞어 보여 주지 않는다.
- 통계의 연결 항목 목록은 처음 4개를 보여 주고 하단에서 10개씩 더 펼친다. 버튼에는 이번에 추가될 개수와 남은 개수를 함께 표시하고, 전부 펼치면 `처음 4개만 보기`로 바뀐다. 펼침·축소는 목록 높이와 행 위치가 부드럽게 이어지며 기간·계획·수치·검색 조건이 바뀌면 다시 4개부터 시작한다.
- 브라우저 저장소·메모리·모의 API만으로 저장 조건을 충족했다고 주장하지 않는다. 실제 서버 DB가 저장의 기준이며 새로고침으로 ID·날짜·값·단위를 대조한다.
- 계획·수정 이력·할 일·실행 기록·완료 사건·돌아보기·다음 계획 연결을 단일 파일로 내보낸다. 삭제는 soft delete로 정의하고 내보내기에는 삭제 표시와 관련 기록도 보존한다.
- 첫 화면에 아래 문구를 그대로 표시한다. 로그인을 붙이지 않고 모든 요구 기능을 사용할 수 있어야 한다.

> 지금은 로그인이 없어 링크를 아는 사람은 누구나 볼 수 있습니다. 남이 봐도 괜찮은 내용만 넣으세요

- 공개 열람과 무인증 변경이 가능한 과제라는 사실을 설계와 안내에 드러낸다. 타인의 이름·연락처 등 다른 사람 정보와 민감한 일기 내용은 넣지 않는다. 잠금과 본인만의 접근 제어는 7번 과제에서 다룬다.
- 최종 DB의 표·항목·타입·관계·제약·날짜/시간대·단위를 `contracts/pds-schema-v2.json`에 기록하고 최종 소스에 포함한다. 설계 초안을 최종 실측 계약이라고 표시하지 않는다.
- 스크립트 모양 문자열을 저장·재표시하여 원문은 유지하고 코드는 실행되지 않음을 검증한다.
- 최종 소스와 브라우저 코드·배포 파일·네트워크 응답·콘솔·Git 전체 이력의 비밀값 노출을 점검한다. 검증 결과에 비밀값 자체를 남기지 않는다.
- 결과물 URL과 소스 URL을 제출한다. 모두 새 시크릿 창에서 계정 생성·로그인·인증·초대·비밀번호·OAuth·CAPTCHA 없이 열려야 한다. 실제 외부 공개 여부를 직접 확인한다.
- 짧은 확인 방법은 어디로 가나요 / 세 단계 안에 무엇을 하나요 / 무엇이 보이면 통과인가요 / 안 될 때는 무엇이 보이나요를 각각 한 줄로 쓴다.
- AI와 내 판단은 AI에게 맡긴 일 / 내가 직접 판단한 일 / AI 제안을 따르지 않은 일(없다면 실제 이유)을 각각 한 줄로 쓴다. AI가 사용자의 판단을 만들어 내지 않는다.
- 계획 시간은 8~10시간이다. 인프라 준비와 본인 실제 자료 확보를 전제로 배분한다. 초과가 필요하면 알리고 조건은 유지한다.

## 증거 보관 의무

공개해도 괜찮은 자료로 아래 증거를 보관한다. 각 증거에는 관련 T06 ID, 배포 커밋, 시각, 기대값·실제값, 통과/실패를 연결한다.

1. 실제 계획 저장 화면과 수정 전후 이력 화면.
2. 할 일 생성·수정·완료·되돌리기·삭제 결과와 검색·필터·정렬 결과.
3. 실행 기록과 할 일의 연결 화면, 연속 두 클릭 후 완료 사건 1건·집계 +1의 DB/API 대조.
4. 집계 화면과 각 숫자에서 도착한 근거 기록, 다음 계획으로 넘어간 한 줄.
5. 사용자가 제공·확인했거나 사용자 승인으로 AI가 입력한 계획·할 일·실행 자료, 사용자가 확인한 0이 아닌 돌아보기, 첫 화면의 공개 안내.
6. 내보낸 단일 파일과 새로고침 전후 ID·날짜·값·단위 비교.
7. 스크립트 문자열 무실행, 다섯 표면의 비밀값 점검, 두 공개 URL 비로그인 접근 결과.

현재 위 증거는 모두 구현 후 확보할 항목이며, 이 설계 작업에서 통과했다고 주장하지 않는다.

## 구현 해석 규칙

- 우선순위는 높음=1, 보통=2, 낮음=3. 예상·실제 시간은 정수 분으로 저장한다. 계획 자체 예상과 할 일 예상 합계는 별도 값이며 자동으로 덮어쓰지 않는다.
- 기간·마감은 서울 달력의 DATE, 실행·변경 시각은 MariaDB DATETIME(6)에 UTC 값으로 저장한다. DATETIME 자체에는 시간대가 없으므로 서버 JDBC 경계에서 UTC 변환을 명시한다. API·내보내기는 UTC ISO 8601 Z, 화면은 Asia/Seoul. 분 단위로 입력하고 초는 00으로 제한하여 반올림 차이를 없앤다.
- 기간 집계는 조회 기간과 현재 계획 기간이 겹치는 계획을 고른 다음, 해당 계획의 미삭제 할 일을 대상으로 계산한다. 계획 ID로도 좁힐 수 있다. 실행은 그 할 일에 연결된 전체 기록을 합한다. 화면에 '계획 기간 기준 · 현재 상태 · 대상 할 일의 전체 실행'이라고 표시한다.
- 집계의 계획 수는 대상 할 일 수다. 완료는 현재 done, 지연은 미완료이고 마감일이 서울 오늘보다 이른 것, 막힘은 공백 제거 후 비어 있지 않은 이유가 1건 이상 있는 고유 할 일 수다. 완료와 막힘은 겹칠 수 있지만 완료와 지연은 겹치지 않는다.
- 집계와 근거 목록은 같은 서버 쿼리·필터를 공유한다. 같은 읽기 스냅샷에서 만든 근거 ID와 합계를 반환하고, 클릭 시 해당 근거를 보여 준다. 최신 재조회로 바뀌면 집계도 함께 갱신한다.
- 완료는 (task_id, cycle)의 유일한 사건이다. 완료 되돌리기는 cycle을 1 올리고 이전 완료 사건·실행을 보존한다. 다시 완료하면 새 cycle 사건 1건만 만든다. 현재 완료 집계는 사건 누적이 아닌 tasks.status에서 계산한다.
- 상세 구현 계약은 `contracts/pds-schema-v2.json`, 기능·검증 설명은 `docs/pds-design.md`와 노션 문서에 둔다. 해석 규칙은 과제 조건을 충족하기 위한 설계 결정이며 원래 조건을 대체하지 않는다.

## 2026-09-08 사용자 추가 설계 제약

- 사용자 후속 결정: 이번 구현에 **공개 과제 공간과 로그인하는 개인 계정 영역을 함께 포함**한다. 위 무인증·공개 안내·제출 조건은 공개 과제 공간에서 전부 유지한다. 개인 영역은 별도 추가 범위이며 공개 경로로 개인 자료에 접근할 수 없어야 한다. 원문의 '7번 과제에서 다룬다'는 접근 제어를 공개 과제의 이용 조건으로 붙이지 않는다는 경계로 유지하고, 별도 개인 영역은 이번에 함께 구현한다.
- 개인 영역은 이메일 가입과 카카오·네이버·구글 로그인을 모두 제공한다. 현재는 본인만 기록을 조회·수정하며 향후 공동 편집 확장을 고려한다. 웹 JDBC 세션·보안 쿠키·CSRF를 채택했고 인증 구현·검증을 진행한다. 기존 아홉 표만으로 인증 설계가 완료됐다고 표시하지 않는다. 목록·상세·이력·집계·근거·변경·관계·요청 키 응답·내보내기에 같은 접근 경계를 적용한다. 상세 결정·AUTH-01~07 추가 검증 추적은 `docs/backend-decisions.md`와 `contracts/pds-schema-v2.json`의 `access_scope_extension`을 따른다.
- 사용자 환경: WTR Pro는 Ryzen 7 5825U·RAM 32GB·6TB×2·2TB×1이며 CCTV용 하드로 24시간 사용 가능하다는 사용자 진술이다. 초기 이용자는 약 10명 예상이다. 실제 가용성·OS·외부 접속·월 예산은 미확인이다. 초기 운영은 클라우드, WTR Pro는 후속 이전 대상이다.


- 기존 공개 웹 과제의 8~10시간에 개인 계정·인증·접근 분리 검증 시간을 포함했다고 주장하지 않는다. 추가 일정은 구현·외부 연동 검증을 반영해 산정하며 모든 과제 조건은 유지한다.

- 서비스 도메인은 사용자가 구매했다고 알린 `plandosee.app`으로 확정한다. 공개 웹 주소는 `https://plandosee.app`, 공통 API 기본 주소는 `https://plandosee.app/api/v1`로 계획한다. 구매 사실은 사용자 진술이며 DNS·HTTPS·백엔드 배포 연결은 확인했다. 프론트는 미구현이다.

- 백엔드는 Spring Boot, DB는 MariaDB로 구상한다. 프론트엔드·백엔드·인프라는 각각 다른 저장소로 관리한다.
- 웹은 Next.js + React + TypeScript로 구현한다. Next.js 기본 빌드 도구를 사용하고 Vite를 별도 결합하지 않는다.
- 앱은 Android 네이티브를 우선한다. Kotlin + Jetpack Compose로 설계하고 웹과 별도 저장소로 관리한다. iOS는 현재 개발·배포 범위에서 제외한다.
- Windows·macOS의 네이티브 앱은 이후 검토하며 현재는 웹을 제공한다. 향후 클라이언트 추가가 가능한 공통 REST API를 유지한다.
- 웹 SSR과 Android 화면은 각각 구현하되 DB·집계·완료 중복 방지·날짜 판정은 공통 Spring Boot API가 담당한다. 웹 8~10시간 목표에 Android 개발·배포 시간을 포함했다고 주장하지 않는다.
- 서버 요금은 사용량·백업·전송량과 운영 부담을 포함해 판단한다. 문서 가격과 실제 구매 가능 여부를 구분한다.
- 이번 변경은 기술 구성의 수정이다. docs/requirements.json의 원문 44개 조건과 번호 없는 필수 요구는 축소하지 않는다.

- 2026-09-08 후속 결정: 사용자가 JDBC·Spring Security·웹 JDBC 세션·외부 SMTP와 휴지통 복원(REC-01)·계획 틀 복제(REC-02)·선택형 알림(REC-03)을 모두 채택했다. n8n은 후속 알림·운영 자동화로 검토하고 인증 메일은 Spring에서 외부 SMTP로 전송한다. 클라우드부터 구현하고 WTR Pro로 이전할 수 있도록 준비한다.
- 사용자가 알려 준 MariaDB 개발 포트는 3919다. 기존 DB에 대한 migration은 연결 대상·계정·전용 스키마를 확인한 뒤 적용한다. 테스트는 격리된 DB에서 실행한다.

## 무료 호스팅·메일 준비·프론트 API 후속 지시

2026-09-08 최신 결정: 사용자가 최종 클라우드 제공자로 Vultr를 선택했다. 사용자는 작은 클라우드 서버로 줄일 필요가 없고 한 달 안에 이전할 것으로 예상한다고 밝혔다. WTR Pro로의 이전을 목표로 서울(icn) Shared CPU AMD High Performance 8vCPU·RAM 16GB·350GB 한 대를 체험용으로 추천하며 공식 카탈로그의 서버 요금은 월 US$96이다. US$250·최대 30일 프로모션의 실제 적용/만료는 확인 전이다. 새 한국 서버 158.247.203.145의 DB·백엔드·Caddy 배포와 서버 SMTP 시험을 확인했다. 공개 HTTPS·API·Swagger는 확인했고 프론트·전체 인수 검증은 남아 있다. 실제 증거는 docs/deployment-status.md를 따른다. Brevo 도메인 Authenticated는 사용자 진술로 확인했다. 발신자 PlanDoSee <no-reply@plandosee.app>의 서버 SMTP 시험을 통과했고, 사용자가 네이버 받은편지함 도착을 확인했다. 소셜 제공자 앱 등록·키·콜백·검수는 프론트 완성 뒤 한 번에 진행한다. 클라우드부터 운영하고 WTR Pro로 후속 이전하는 방침을 유지한다. 구상 저장소의 OpenAPI 0.2.0·Swagger UI·프론트 안내는 42개 경로·59개 작업·38개 모델·예시 43개와 소스 대조/브라우저 검사를 통과했다.

소셜 로그인은 필수 범위를 유지하며 외부 연동 시점만 프론트 이후로 조정한다. 원문 44개와 번호 없는 조건은 변경하지 않는다.

[사용자 준비물 한 번에 보기](docs/user-preparation.md) · [프론트 연동 안내](docs/frontend-integration.md) · [Swagger 실행](docs/swagger/README.md) · [무료 호스팅 비교](docs/cloud-options.md)
