# PDS Diary 필수 RULE

2026-09-09 최신 상태: 공개/개인 웹을 [운영 주소](https://plandosee.app)에 배포했다. 계획/이력·할 일·실행/완료·검색/필터/정렬·돌아보기/근거·다음 계획·전체 JSON·휴지통/복제·이메일 계정·알림 설정을 연결했다. 타입/Windows·Linux 빌드, 단위13·격리 API/DB17·운영 HTTPS16개를 통과했다. 사용자 최신 지시로 소셜 로그인은 명시적으로 다시 요청할 때까지 작업을 보류한다. 실제 사용자 1/5/3·최종 DB 계약·전체 인수는 미완료. 상세: [웹 구현·검증](../PDC_Diary_Nextjs/docs/full-implementation.md). 아래 이전 단계 기록은 당시 상태다.

최신 배포 증거: [DB·백엔드·메일 배포 진행](docs/deployment-status.md) — 내부 API·21개 표 복원·메일 수신 확인, 공개 DNS/HTTPS·API·Swagger 확인 완료, 프론트는 미구현.


기준일: 2026-09-08 / 상태: 백엔드 구현·로컬 시험 진행, 배포·전체 과제·실제 사용자 기록 검증 전. 실제 증거는 docs/backend-verification.md에 기록한다.

## 효력과 완료 판정

1. 사용자가 제공한 아래 조건은 모두 필수다. 중요도나 시간 부족을 이유로 하나도 생략·완화하지 않는다.
2. 이 문서는 개발 규칙이다. RULE 문서만 있다고 충족된 것이 아니며, 실제 배포 결과와 증거가 있어야 통과다.
3. 추적 상태는 `미구현 → 구현됨 → 검증됨`으로 관리한다. 실패·미확인은 별도 기록한다. 44개 조건 및 번호 없는 조건 중 하나라도 미검증·실패이면 과제 전체 완료로 보고하지 않는다.
4. 설명 없는 번호를 임의로 채우지 않는다. 제공된 ID는 C01, C04~C36, C57~C60, C78~C83의 총 44개다.
5. [요구사항 원문 및 검증 연결](docs/requirements.json)의 44개 `text`는 본 RULE의 일부다. 모든 항목은 동일한 강제력을 가진다. 변경하려면 사용자 요구 변경을 명시적으로 확인하고 문서·계약·검증을 함께 갱신한다.

## 문서·노션 동기화

2026-09-08 사용자 지시: 앞으로 계획 등을 수정할 때는 로컬 문서뿐 아니라 노션도 함께 수정한다.

- 계획·기능·기술·인프라·일정·RULE 변경 시 관련 로컬 Markdown·JSON 문서와 해당 노션 하위 문서를 같은 작업에서 갱신한다. 내용이 영향받는 계약도 함께 맞춘다.
- 2026-09-08 사용자의 후속 지시로 PDF를 삭제하고 관리 대상에서 제외한다. 사용자가 다시 요청하기 전에는 PDF를 생성·갱신·검증하지 않는다. `docs/pds-design.md`는 직접 편집하고 이전 PDF 생성·검증 스크립트는 실행하지 않는다.
- 노션 목차: https://app.notion.com/p/3d50def9f6268037adfbcef233c6d20c . 하위 문서 URL·ID는 `docs/notion-publication.json`에서 확인한다. 목차 구조가 바뀌면 링크도 함께 갱신한다.
- 노션의 최신 내용을 먼저 읽어 사용자가 직접 수정한 내용을 보존한다. 필요한 부분만 간결하게 수정하고 저장 후 재조회하여 로컬 문서와 일치하는지 확인한다.
- 변경된 노션 문안은 `docs/notion-content.json`에 반영하고, 동기화 결과·변경 페이지는 `docs/notion-publication.json`에 기록한다.
- 사용자 후속 지시: 설명과 중간 결과를 자주 제공하고 매 작업 단계의 결정·변경·실제 검증 결과·남은 일을 로컬 문서와 노션에 항상 기록한다. 사용자 확정과 AI 추천을 구분하며 비밀값·개인 기록 원문은 진행 기록에 복사하지 않는다.
- 접속·권한·저장 오류로 한쪽이 갱신되지 않으면 미동기화 항목과 이유를 기록하고 사용자에게 알린다. 양쪽 반영 확인 전에는 문서 수정 완료로 보고하지 않는다.

## AI 작업 시 문맥 확인 절차

2026-09-09 사용자 후속 지시에 따른 필수 작업 규칙. 설계·구현·검증·제출 작업에 적용한다.

1. 대상 저장소의 적용 AGENTS.md와 중앙 RULES.md를 읽는다. 공통 현재 설계·사용자 후속 결정·작업 도메인의 계약 원문도 확인한다.
2. 저장소 목록과 변경 영역으로 관련 저장소를 정한다. 기본 설계와 대상 구현 저장소를 함께 참고하고, 관련 프런트·백엔드·Security·Optimization의 채택된 결정이 있으면 해당 원문을 추가로 읽는다. 저장소 개수로 필요한 근거를 제한하지 않는다.
3. 파일 목록·키워드 검색으로 필요한 원문을 찾고, 정본·현재 작업 버전·미커밋 변경·현재/과거 상태를 구분한다. 필수 RULE을 검색 상위 결과나 요약으로 대체하지 않는다. 다른 저장소의 문서를 링크만 보고 읽었다고 간주하지 않는다.
4. 수정 전에 읽은 주요 출처와 적용할 조건·변경 범위를 간결하게 밝힌다. 필요한 원문이 없거나 서로 충돌하면 불확실성을 알리고, 해결되지 않은 조건에 의존한 수정을 추측으로 진행하지 않는다.
5. 작업 재개·문맥 압축·설계 변경 후에는 변경 대상의 필수 조건과 현재 원문을 다시 확인한다. 과거 대화의 기억만으로 규칙 준수를 판단하지 않는다.
6. 변경 후 관련 계약·회귀 검사를 실행하고 실제 결과를 기록한다. 문서만 수정한 경우에는 원문/링크·동기화 재조회 등 해당 변경에 맞는 확인을 한다. 적용 조건·출처·검사 결과·남은 일과 변경된 설계 문서를 로컬/노션에 함께 반영한다.

기본 방식은 **필수 원문 직접 읽기 + 관련 파일 검색 + 변경 후 검증**이다. 별도 벡터 RAG 구축은 필수 준비물이 아니며, 근거 누락·탐색 비용이 실제로 커지면 추가 효과를 평가한다. 상세 설명과 과거 제안은 docs/context-management.md에 둔다.

## 번호 없는 필수 조건

- Plan → Do → See → 다음 Plan이 하나로 이어져야 한다. 체크 완료만 있는 도구로 끝내지 않는다.
- 실제로 하고 있는 운동·공부·업무·게임 연습·ALEPH 진행 중 하나를 본인이 선택한다. 본인 계획 1개 이상, 그 계획의 실제 할 일 5개 이상, 실제 실행 기록 3개 이상을 저장한다. 타인 예시·AI 창작·검사용 데이터를 실제 기록으로 세지 않는다.
- 계획 필드는 ID·기간·우선순위·성공 기준·예상 시간을 구현 전에 정한다. 같은 계획 ID를 유지하고 현재 값과 수정 이력을 분리한다. 최초 버전과 수정 전 버전은 불변이다.
- 할 일은 계획에 연결하고 생성·수정·완료·되돌리기·삭제·검색·필터·정렬을 제공한다. 마감일·우선순위·태그·예상 시간을 저장한다. 정렬 기준과 동률 처리 기준을 화면에 공개하고 서버에서 일관되게 적용한다.
- 시작·종료·실제 소요 시간·막힌 이유를 할 일에 연결한 별도 실행 기록으로 저장한다. 실행 저장은 계획과 할 일 예상값을 변경하지 않는다.
- 완료 버튼 연속 두 클릭을 직접 검증한다. 같은 요청 키, DB 고유 제약, 원자적 상태 변경으로 중복을 막는다. 버튼 비활성화만으로 처리하지 않는다. 완료 사건과 완료 집계가 각각 한 번만 증가해야 한다.
- 기간별 계획 수·완료 수·지연 수·막힘 수·예상 시간·실제 시간·차이를 표시한다. 숫자는 근거 기록으로 이동해야 한다. 고칠 점 한 줄을 다음 계획에 출처와 함께 넘긴다.
- 브라우저 저장소·메모리·모의 API만으로 저장 조건을 충족했다고 주장하지 않는다. 실제 서버 DB가 저장의 기준이며 새로고침으로 ID·날짜·값·단위를 대조한다.
- 계획·수정 이력·할 일·실행 기록·완료 사건·돌아보기·다음 계획 연결을 단일 파일로 내보낸다. 삭제는 soft delete로 정의하고 내보내기에는 삭제 표시와 관련 기록도 보존한다.
- 첫 화면에 아래 문구를 그대로 표시한다. 로그인을 붙이지 않고 모든 요구 기능을 사용할 수 있어야 한다.

> 지금은 로그인이 없어 링크를 아는 사람은 누구나 볼 수 있습니다. 남이 봐도 괜찮은 내용만 넣으세요

- 공개 열람과 무인증 변경이 가능한 과제라는 사실을 설계와 안내에 드러낸다. 타인의 이름·연락처 등 다른 사람 정보와 민감한 일기 내용은 넣지 않는다. 잠금과 본인만의 접근 제어는 7번 과제에서 다룬다.
- 최종 DB의 표·항목·타입·관계·제약·날짜/시간대·단위를 `contracts/pds-schema-v2.json`에 기록하고 최종 소스에 포함한다. 설계 초안을 최종 실측 계약이라고 표시하지 않는다.
- 스크립트 모양 문자열을 저장·재표시하여 원문은 유지하고 코드는 실행되지 않음을 검증한다.
- 최종 소스와 브라우저 코드·배포 파일·네트워크 응답·콘솔·Git 전체 이력의 비밀값 노출을 점검한다. 검증 결과에 비밀값 자체를 남기지 않는다.
- 결과물 URL과 소스 URL을 제출한다. 모두 새 시크릿 창에서 계정 생성·로그인·인증·초대·비밀번호·OAuth·CAPTCHA 없이 열려야 한다. 실제 외부 공개 여부를 직접 확인한다.
- 짧은 확인 방법은 어디로 가나요 / 세 단계 안에 무엇을 하나요 / 무엇이 보이면 통과인가요 / 안 될 때는 무엇이 보이나요를 각각 한 줄로 쓴다.
- AI와 내 판단은 AI에게 맡긴 일 / 내가 직접 판단한 일 / AI 제안을 따르지 않은 일(없다면 실제 이유)을 각각 한 줄로 쓴다. AI가 사용자의 판단을 만들어 내지 않는다.
- 계획 시간은 8~10시간이다. 인프라 준비와 본인 실제 자료 확보를 전제로 배분한다. 초과가 필요하면 알리고 조건은 유지한다.

## 증거 보관 의무

공개해도 괜찮은 자료로 아래 증거를 보관한다. 각 증거에는 관련 T06 ID, 배포 커밋, 시각, 기대값·실제값, 통과/실패를 연결한다.

1. 실제 계획 저장 화면과 수정 전후 이력 화면.
2. 할 일 생성·수정·완료·되돌리기·삭제 결과와 검색·필터·정렬 결과.
3. 실행 기록과 할 일의 연결 화면, 연속 두 클릭 후 완료 사건 1건·집계 +1의 DB/API 대조.
4. 집계 화면과 각 숫자에서 도착한 근거 기록, 다음 계획으로 넘어간 한 줄.
5. 본인 계획·할 일·실행 자료, 0이 아닌 돌아보기, 첫 화면의 공개 안내.
6. 내보낸 단일 파일과 새로고침 전후 ID·날짜·값·단위 비교.
7. 스크립트 문자열 무실행, 다섯 표면의 비밀값 점검, 두 공개 URL 비로그인 접근 결과.

현재 위 증거는 모두 구현 후 확보할 항목이며, 이 설계 작업에서 통과했다고 주장하지 않는다.

## 구현 해석 규칙

- 우선순위는 높음=1, 보통=2, 낮음=3. 예상·실제 시간은 정수 분으로 저장한다. 계획 자체 예상과 할 일 예상 합계는 별도 값이며 자동으로 덮어쓰지 않는다.
- 기간·마감은 서울 달력의 DATE, 실행·변경 시각은 MariaDB DATETIME(6)에 UTC 값으로 저장한다. DATETIME 자체에는 시간대가 없으므로 서버 JDBC 경계에서 UTC 변환을 명시한다. API·내보내기는 UTC ISO 8601 Z, 화면은 Asia/Seoul. 분 단위로 입력하고 초는 00으로 제한하여 반올림 차이를 없앤다.
- 기간 집계는 조회 기간과 현재 계획 기간이 겹치는 계획을 고른 다음, 해당 계획의 미삭제 할 일을 대상으로 계산한다. 계획 ID로도 좁힐 수 있다. 실행은 그 할 일에 연결된 전체 기록을 합한다. 화면에 '계획 기간 기준 · 현재 상태 · 대상 할 일의 전체 실행'이라고 표시한다.
- 집계의 계획 수는 대상 할 일 수다. 완료는 현재 done, 지연은 미완료이고 마감일이 서울 오늘보다 이른 것, 막힘은 공백 제거 후 비어 있지 않은 이유가 1건 이상 있는 고유 할 일 수다. 완료와 막힘은 겹칠 수 있지만 완료와 지연은 겹치지 않는다.
- 집계와 근거 목록은 같은 서버 쿼리·필터를 공유한다. 같은 읽기 스냅샷에서 만든 근거 ID와 합계를 반환하고, 클릭 시 해당 근거를 보여 준다. 최신 재조회로 바뀌면 집계도 함께 갱신한다.
- 완료는 (task_id, cycle)의 유일한 사건이다. 완료 되돌리기는 cycle을 1 올리고 이전 완료 사건·실행을 보존한다. 다시 완료하면 새 cycle 사건 1건만 만든다. 현재 완료 집계는 사건 누적이 아닌 tasks.status에서 계산한다.
- 상세 구현 계약은 `contracts/pds-schema-v2.json`, 기능·검증 설명은 `docs/pds-design.md`와 노션 문서에 둔다. 해석 규칙은 과제 조건을 충족하기 위한 설계 결정이며 원래 조건을 대체하지 않는다.

## 2026-09-08 사용자 추가 설계 제약

- 사용자 후속 결정: 이번 구현에 **공개 과제 공간과 로그인하는 개인 계정 영역을 함께 포함**한다. 위 무인증·공개 안내·제출 조건은 공개 과제 공간에서 전부 유지한다. 개인 영역은 별도 추가 범위이며 공개 경로로 개인 자료에 접근할 수 없어야 한다. 원문의 '7번 과제에서 다룬다'는 접근 제어를 공개 과제의 이용 조건으로 붙이지 않는다는 경계로 유지하고, 별도 개인 영역은 이번에 함께 구현한다.
- 개인 영역은 이메일 가입과 카카오·네이버·구글 로그인을 모두 제공한다. 현재는 본인만 기록을 조회·수정하며 향후 공동 편집 확장을 고려한다. 웹 JDBC 세션·보안 쿠키·CSRF를 채택했고 인증 구현·검증을 진행한다. 기존 아홉 표만으로 인증 설계가 완료됐다고 표시하지 않는다. 목록·상세·이력·집계·근거·변경·관계·요청 키 응답·내보내기에 같은 접근 경계를 적용한다. 상세 결정·AUTH-01~07 추가 검증 추적은 `docs/backend-decisions.md`와 `contracts/pds-schema-v2.json`의 `access_scope_extension`을 따른다.
- 사용자 환경: WTR Pro는 Ryzen 7 5825U·RAM 32GB·6TB×2·2TB×1이며 CCTV용 하드로 24시간 사용 가능하다는 사용자 진술이다. 초기 이용자는 약 10명 예상이다. 실제 가용성·OS·외부 접속·월 예산은 미확인이다. 초기 운영은 클라우드, WTR Pro는 후속 이전 대상이다.


- 기존 공개 웹 과제의 8~10시간에 개인 계정·인증·접근 분리 검증 시간을 포함했다고 주장하지 않는다. 추가 일정은 구현·외부 연동 검증을 반영해 산정하며 모든 과제 조건은 유지한다.

- 서비스 도메인은 사용자가 구매했다고 알린 `plandosee.app`으로 확정한다. 공개 웹 주소는 `https://plandosee.app`, 공통 API 기본 주소는 `https://plandosee.app/api/v1`로 계획한다. 구매 사실은 사용자 진술이며 DNS·HTTPS·백엔드 배포 연결은 확인했다. 프론트는 미구현이다.

- 백엔드는 Spring Boot, DB는 MariaDB로 구상한다. 프론트엔드·백엔드·인프라는 각각 다른 저장소로 관리한다.
- 웹은 Next.js + React + TypeScript로 구현한다. Next.js 기본 빌드 도구를 사용하고 Vite를 별도 결합하지 않는다.
- 앱은 Android 네이티브를 우선한다. Kotlin + Jetpack Compose로 설계하고 웹과 별도 저장소로 관리한다. iOS는 현재 개발·배포 범위에서 제외한다.
- Windows·macOS의 네이티브 앱은 이후 검토하며 현재는 웹을 제공한다. 향후 클라이언트 추가가 가능한 공통 REST API를 유지한다.
- 웹 SSR과 Android 화면은 각각 구현하되 DB·집계·완료 중복 방지·날짜 판정은 공통 Spring Boot API가 담당한다. 웹 8~10시간 목표에 Android 개발·배포 시간을 포함했다고 주장하지 않는다.
- 서버 요금은 사용량·백업·전송량과 운영 부담을 포함해 판단한다. 문서 가격과 실제 구매 가능 여부를 구분한다.
- 이번 변경은 기술 구성의 수정이다. docs/requirements.json의 원문 44개 조건과 번호 없는 필수 요구는 축소하지 않는다.

- 2026-09-08 후속 결정: 사용자가 JDBC·Spring Security·웹 JDBC 세션·외부 SMTP와 휴지통 복원(REC-01)·계획 틀 복제(REC-02)·선택형 알림(REC-03)을 모두 채택했다. n8n은 후속 알림·운영 자동화로 검토하고 인증 메일은 Spring에서 외부 SMTP로 전송한다. 클라우드부터 구현하고 WTR Pro로 이전할 수 있도록 준비한다.
- 사용자가 알려 준 MariaDB 개발 포트는 3919다. 기존 DB에 대한 migration은 연결 대상·계정·전용 스키마를 확인한 뒤 적용한다. 테스트는 격리된 DB에서 실행한다.

## 무료 호스팅·메일 준비·프론트 API 후속 지시

2026-09-08 최신 결정: 사용자가 최종 클라우드 제공자로 Vultr를 선택했다. 사용자는 작은 클라우드 서버로 줄일 필요가 없고 한 달 안에 이전할 것으로 예상한다고 밝혔다. WTR Pro로의 이전을 목표로 서울(icn) Shared CPU AMD High Performance 8vCPU·RAM 16GB·350GB 한 대를 체험용으로 추천하며 공식 카탈로그의 서버 요금은 월 US$96이다. US$250·최대 30일 프로모션의 실제 적용/만료는 확인 전이다. 새 한국 서버 158.247.203.145의 DB·백엔드·Caddy 배포와 서버 SMTP 시험을 확인했다. 공개 HTTPS·API·Swagger는 확인했고 프론트·전체 인수 검증은 남아 있다. 실제 증거는 docs/deployment-status.md를 따른다. Brevo 도메인 Authenticated는 사용자 진술로 확인했다. 발신자 PlanDoSee <no-reply@plandosee.app>의 서버 SMTP 시험을 통과했고, 사용자가 네이버 받은편지함 도착을 확인했다. 소셜 제공자 앱 등록·키·콜백·검수는 프론트 완성 뒤 한 번에 진행한다. 클라우드부터 운영하고 WTR Pro로 후속 이전하는 방침을 유지한다. 구상 저장소의 OpenAPI 0.2.0·Swagger UI·프론트 안내는 42개 경로·59개 작업·38개 모델·예시 43개와 소스 대조/브라우저 검사를 통과했다.

소셜 로그인은 필수 범위를 유지하며 외부 연동 시점만 프론트 이후로 조정한다. 원문 44개와 번호 없는 조건은 변경하지 않는다.

[사용자 준비물 한 번에 보기](docs/user-preparation.md) · [프론트 연동 안내](docs/frontend-integration.md) · [Swagger 실행](docs/swagger/README.md) · [무료 호스팅 비교](docs/cloud-options.md)
