# 현재 웹 → Figma 편집 → 웹 반영

최신 상태: 로그인 수정본과390px 모바일6상태 전송 완료 응답을 확인했다. 채택 캡처12개와 교체 전 초기7개가 있으며 초기본 제거는 대기다. 원본 브라우저의 로그인/모바일4조건에서 기대 너비·가로 넘침 없음·연결 오류 없음, 로그인 비밀번호 입력란 표시를 확인했다. 원본 모바일 펼침과 로그인 이미지를 검토했다. Figma 내부 수정본 최종 시각 검수는 MCP 한도 때문에 미실행이다. 사용자가 ‘내가 Figma에서 요금제를 확인·변경할게’라고 답했으며 완료는 아직 미확인이다. 한도 해소 후 컴포넌트 통합·페이지 정리·날짜 입력 표시·초기본 삭제·최종 검수를 이어간다. 현재 전송본은 중간 산출물이다.

2026-09-10 사용자 요청: 현재 웹을 Figma에서 편집 가능한 화면으로 옮기고, 이후 사용자가 수정한 Figma를 읽어 웹에 반영한다. 자동 양방향 동기화는 구현하지 않으며 사용자 요청 시 변경된 프레임을 확인해 코드를 수정·검증한다. 사용자는 필요한 승인은 구체적으로 질문하라고 지시했다.

새 파일: https://www.figma.com/design/UrFtdh7olajlLI2WP80X4P — 사용자 계정 whoami에서 확인한 단일 팀의 drafts에 생성. 화면 전송 진행 중이며 최종 제작 완료가 아니다.

기준: 웹 배포 b377d4c·현재 pds-design.md·RULES.md·웹 AGENTS·globals.css의 DM Sans/Noto Sans KR와 6개 기본 색상. 원문44/번호 없는18·API/DB 계약·소셜 보류를 유지한다. 이번 범위는 디자인 산출물이며 운영 코드·배포·실제 기록을 변경하지 않는다.

확인: 새 파일에 기존 화면/컴포넌트/로컬 변수 없음. 소스 Code Connect 파일 없음. 라이브러리 조회 후 PlanDoSee 컴포넌트·변수·스타일 검색 각각0건. 일반 커뮤니티 라이브러리는 현재 웹의 정본이 아니므로 기존 CSS를 따른다. Figma에 DM Sans와 Noto Sans KR가 제공됨을 확인했다.

AI 제작 범위: 랜딩·Plan·Do·See·로그인, 주요 모바일 및 할 일 추가 펼침 상태. 기존 웹 색상과 타이포그래피, 반복 버튼·태그·입력란의 편집 가능한 컴포넌트. 전체 기능의 서버 동작을 Figma에서 재구현하는 것은 아니다.

첫 generate_figma_design 호출은 운영 개인 기록 전송 가능성과 목적지 소유 근거 부족을 이유로 자동 검토에서 거절됐다. 실제 기록 대신 코드에 적은 독서 예시만 반환하는 127.0.0.1:8094 API와 4173 미리보기를 준비했다. 원본 웹 빌드는3003에서 해당 예시 API만 조회한다. 모든 저장 요청405·DB 연결 없음. 2026-09-10T00:08:03.704Z 브라우저에서 Plan의 세 예시 행·인라인 추가 표시와 운영 요청0을 확인하고 이미지를 검토했다.

전송 대상과 내용이 정해진 뒤 구체적 승인 질문을 보냈고, 사용자가 ‘실제 웹 기록도 포함해서 전송·제작 승인’이라고 답했다. 지정 파일에 실제 공개 화면을 전송했다. 개인 계정 쿠키를 전달하지 않는 공개 GET 전용 미리보기를 사용하며 저장 요청은 차단한다. 로그인 화면 표시를 위한 공개 auth/providers GET만 추가했다. 운영 자료·코드는 수정하지 않았다.

진행: 색상 컬렉션2개·변수12개(원색6/의미6)·각1모드, 글꼴4개, Desktop/Mobile/공통 요소3페이지, 안내·색상 표본·기본 버튼1개를 생성했다. DM Sans/Noto Sans KR 제공 여부와 변수 원문 값을 확인했다. 랜딩·Plan·Do·See·인라인 추가 화면의 편집 가능한 레이어 전송을 확인했으며 안내와 Plan 인라인 화면 이미지를 확인했다.

검수 발견: 초기 로그인 캡처는 미리보기 조회 제한으로 오류 화면이었으며, 초기 모바일은 브라우저 최소 너비 때문에492px이었다. auth/providers 조회와390px 화면 크기를 설정해 수정 캡처를 진행한다. 일부 HTML 날짜 입력값은 캡처에서 표시되지 않아 Figma에서 보완이 필요하다.

현재 제약: Figma가 Starter 요금제의 MCP 호출 한도 도달을 응답했다. 기본 버튼 시각 검증·태그/입력 컴포넌트·화면 인스턴스 연결·페이지 정리·잘못된 초기 캡처 제거·최종 시각 검수가 미완료다. 사용자에게 요금제 확인/변경 후 계속할지, 현재 전송 화면까지만 정리할지 질문했다. 유료 변경은 실행하지 않았다. 상세 ID는 figma-node-ledger.json에 보존한다.
