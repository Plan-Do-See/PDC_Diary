# 계획 안에서 할 일 추가

## 2026-09-09 · 인라인 할 일 추가 운영 배포 완료

사용자 승인 초안대로 계획 안에서 작은 추가 입력란이 펼쳐지며 저장 후 같은 목록을 갱신하고 제목만 비워 연속 입력한다. 마감·필수 태그·접히는 우선순위/예상 분·계획별 입력 보존을 구현했다. 배포 커밋 b377d4cb9d3b1ff3fbaa2a45824d28e95dde732f, 릴리스 20260909T0840Z.

타입·단위18·Windows/Linux 빌드 및 격리 실제 API/DB10항목 검증 통과. 운영 비로그인 HTTP7개200, 펼침/닫기 입력 보존·추가 설정·서울 오늘 기본값·개인 로그인 경계 확인. 1280/696/390/320px 넘침 없음, 페이지/콘솔 오류0, 운영 POST0. 전체62개 배포 입력은 공개 GitHub 커밋과 대조했으며 동일 묶음 SHA를 서버에서도 확인했다. 웹 healthy·node·read-only·1GiB, API/DB 컨테이너 유지와 릴리스 백업을 확인했다.

초기 전송 자동 검토가 구체적 payload/목적지 승인을 이유로 거절했으나,62파일 공개 HTTP200/내용 일치 및 기존 서버 조회 증거를 추가한 동일 전송 재검토에서 승인되어 배포 완료했다. 초기에 Next 동적 경로를 검사기가 잘못 해석한 문제는 검사식을 교정한 뒤 전부 대조했다. 운영 기록은 변경하지 않았다. 원문44/번호 없는18·API/DB 계약·소셜 보류를 유지한다. 실제 사용자1/5/3·최종 DB 계약·전체 인수는 미완료다. 상세 검증은 웹 docs/inline-task-verification.json, 배포는 web-release.json과 release-source-manifest.json을 따른다.


## 2026-09-09 · 인라인 할 일 추가 채택·구현 검증

사용자가 ‘그대로 구현해서 배포까지’ 승인했다. 계획의 추가 버튼이 작은 입력란으로 펼쳐지고 이름·마감일·필수 태그를 입력한다. 우선순위·예상 분은 추가 설정에서 펼친다. 첫 열기 서울 오늘·보통·0분 기본값을 채택했다. 닫기/재열기와 다른 계획 선택에도 입력을 보존하고, 저장 후 제목만 비워 같은 계획에 연속 추가한다. 서버 정렬로 목록과 소속 개수·예상 합계를 갱신한다.

중앙 RULES.md·설계 초안·웹 AGENTS·Spring OpenAPI TaskInput/목록 원문을 적용했다. 태그1~30개, 같은 공간의 계획 연결, 정수 분, 공개/개인 CSRF 경계를 유지한다. 생성 API는 멱등 키 보장을 제공하지 않으므로 응답 불확실 시 자동 재전송하지 않고 기록 확인을 안내한다.

실제 확인: 타입·기존 단위18·Windows 빌드 통과. 격리 Spring/MariaDB에서 필수 태그, 계획별 초안 보존, 두 클릭1요청, 저장값/소속, 연속 입력, 서버 순서/계획 예상 유지, 오류 입력 보존, 응답 유실 뒤 실제1건/자동 재전송 없음, 새로고침, 개인 생성/공개 제외의10항목 통과. 1280/696/390/320px 넘침 없음, 페이지 오류0. 모바일 터치 화면과 접힌 잘못된 예상 시간 노출도 확인했다. 합성 검사이며 운영 기록은 변경하지 않았다.

현재 Linux 빌드·운영 배포·운영 읽기 검증을 진행한다. 원문44/번호 없는18·API/DB 계약·소셜 보류를 유지하며 실제 사용자1/5/3·최종 DB 계약·전체 인수는 미완료다.


## 2026-09-09 · 계획 안에서 펼치는 할 일 추가 초안

사용자 요청: 할 일 추가 때 Do로 이동하지 않고 버튼 영역이 작게 확장되는 초안을 요청했다. 계획 머리말과 기존 할 일 아래에 작은 입력란을 붙이고, 이름·마감일·필수 태그를 기본으로 표시했다. 우선순위·예상 시간은 추가 설정에서 펼치며 현재값은 접힌 상태에서도 보인다. 태그1~30개와 정수 분·기존 계획 연결 계약을 유지한다. 날짜 기본 오늘·우선순위 보통·예상0분은 이 초안의 제안이며 운영 변경은 아니다.

추가하면 같은 계획에 새 행을 넣고 제목만 비워 연속 입력한다. 닫기/다시 열기로 초안을 보존하며 다른 계획 입력과 분리한다. 200ms 펼침과 동작 줄이기 설정을 고려했다. 대화용 예시만 사용하며 API/DB·운영 소스·배포는 변경하지 않았다.

검증: 독립 문서의 Edge에서 필수 태그·같은 부모에 추가·페이지 이동 없음·닫기/재열기 입력 보존·추가 설정 보존·연속 입력·스크립트 문자열 무실행을 확인했다. 밝음/어두움 ×736/360/320px 가로 넘침 없음, JavaScript 오류0. 데스크톱과 모바일 이미지를 확인했다. 첫 검사에서는 미리보기 iframe 바깥을 조회해 중단되어 독립 문서 검사로 수정했다. 호스트 디자인 조절 패널의 실제 조작은 미검증이다.

이 초안은 AI 배치 제안이며 운영 구현은 후속이다. 소셜 보류·원문44/번호 없는18·API/DB 계약·실제 사용자 기록/최종 인수 미완료를 유지한다.
